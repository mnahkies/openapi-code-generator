{
  "name": "openapi-code-generator-root",
  "private": true,
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "refresh": "./scripts/refresh-data.sh",
    "start": "nodemon",
    "analyse": "eslint ./packages/**/src --cache --fix --ext .js,.jsx,.ts,.tsx",
    "build": "lerna run build",
    "generate": "lerna run generate",
    "validate": "lerna run validate",
    "ci-build": "lerna run ci-build",
    "ci-test": "lerna run ci-test",
    "ci-analyse": "eslint ./packages/**/src --ext .js,.jsx,.ts,.tsx",
    "ci-pipeline": " lerna run ci-pipeline && yarn generate && yarn validate"
  },
  "author": "Michael Nahkies",
  "license": "UNLICENSED",
  "devDependencies": {
    "@istanbuljs/nyc-config-typescript": "^1.0.1",
    "@types/mocha": "^7.0.2",
    "@types/chai": "^4.2.11",
    "@types/node": "^14.0.13",
    "@typescript-eslint/eslint-plugin": "^3.2.0",
    "@typescript-eslint/parser": "^3.2.0",
    "chai": "^4.2.0",
    "mocha": "^7.2.0",
    "mocha-teamcity-reporter": "^3.0.0",
    "eslint": "^7.2.0",
    "eslint-plugin-import": "^2.21.2",
    "lerna": "^3.22.1",
    "nodemon": "^2.0.4",
    "nyc": "^15.1.0",
    "source-map-support": "^0.5.19",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.5"
  },
  "dependencies": {},
  "workspaces": [
    "packages/*",
    "integration-tests/*"
  ]
}
